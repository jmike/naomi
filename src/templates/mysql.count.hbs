SELECT COUNT(*) AS `count` FROM {{escape table}}

{{#if where}}
  WHERE {{where}}
{{/if}}

{{#if orderBy}}
  ORDER BY {{orderBy}}
{{/if}}

{{#if limit}}
  LIMIT {{limit}}
  {{#if offset}}
    OFFSET {{offset}}
  {{/if}}
{{/if}}
