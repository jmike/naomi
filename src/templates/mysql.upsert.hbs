INSERT

{{#unless duplicateKeyAssignments}}
  IGNORE
{{/unless}}

INTO {{escape table}} ({{columns}})

VALUES {{values}}

{{#if duplicateKeyAssignments}}
  ON DUPLICATE KEY UPDATE {{duplicateKeyAssignments}}
{{/if}}
